# CLAUDE.md — 수학 전투 RPG 개발 지침

> 이 문서는 Claude Code가 이 프로젝트에서 코드를 작성/수정할 때 항상 따라야 하는 지침입니다.
> 세부 수치·시스템 설계는 이 문서가 아니라 `Docs/수학로그라이크_기획서_updated__1_3.md`(이하 "기획서")가 원본이고,
> 세계관·인물 서사·챕터 스토리·엔딩은 `Docs/수학로그라이크_스토리_2.md`(이하 "스토리 문서")가 원본입니다.
> 이 문서와 기획서가 충돌하면 **기획서가 우선**하며, 이 문서를 기획서에 맞게 갱신해야 합니다.
>
> **최종 동기화:** 2026-08-24 (기획서 대개정 반영 — 마나 시스템 폐지, 계산기 무제한화, 스토리 문서 분리)

---

## 1. 프로젝트 개요

- **장르:** 2D 전투 RPG (로그라이크 아님 — 죽어도 처음부터 다시 시작하지 않고, 리스폰/재도전하되 진행은 유지되는 구조. 기획서 9장 참고)
- **타겟:** 수포자(수학을 포기한 사람들)
- **핵심 루프:** 수학 문제를 풀어 전투력을 얻고, 그 힘으로 몬스터/보스와 싸운다
- **목표 철학:** 처음엔 부담 없는 난이도로 시작해서, 플레이하는 동안 자연스럽게 수학 개념을 익히며 "배움의 재미"와 "전투의 재미"를 동시에 느끼게 한다.
- **개발 우선순위:** 전투/타격감이 수학 문제보다 우선순위가 높다. "수학 문제가 곁들여진 진짜 액션 게임"이 목표 포지셔닝 — 문제 로직 없이 순수 액션(평타-스킬-회피 루프)만으로도 재미가 검증되어야 한다. (기획서 5-7)
- **개발 환경:** Unity (C#), 2D

---

## 2. 개발 진행 원칙 (반드시 준수)

### 2-1. 큰 수정 시 사전 확인
여러 파일에 걸친 수정, 핵심 시스템(전투 로직, 데이터 구조, 저장 시스템 등) 변경, 혹은 기존 기능의 동작 방식을 바꾸는 수정이 필요할 경우, 먼저 어떤 부분을 왜 수정하려는지 설명하고 사용자의 승인을 받은 후 진행한다.
단순 버그 픽스, 오타 수정처럼 영향 범위가 명확히 작은 경우는 바로 진행해도 된다.

### 2-2. 기능별 모듈화
절대 하나의 거대한 스크립트에 모든 로직을 넣지 않는다. 최소한 아래와 같이 폴더/네임스페이스 단위로 분리한다:

```
Combat/       - 전투 시스템 (진행, 데미지 계산, 히트박스, 상태이상, 타격감 레이어 등)
MathQuiz/     - 수학 문제 출제·선별, 난이도 조절(숙련도 연동), 채점 로직
Player/       - 플레이어 스탯, 성장(숙련도), 인벤토리
Enemy/        - 일반 몬스터 및 보스 AI, 패턴, 등급, 상성
Progression/  - RPG 진행, 맵/마을/노드 구조, 약점 보충 노드(개념 트리)
UI/           - 각 화면별 UI 컨트롤러 (계산기 블록 입력 UI 포함)
Data/         - ScriptableObject 기반 데이터 정의 (문제 세트, 몬스터 스탯, 개념 트리 등)
Core/         - 게임 매니저, 이벤트 시스템 등 공통 기반
```

클래스 하나가 여러 책임을 갖지 않도록 하고, 시스템 간에는 이벤트나 인터페이스로 느슨하게 연결한다.

### 2-3. 모호한 요청 처리
요청이 모호하거나 여러 해석이 가능할 경우, 임의로 추론해서 진행하지 말고, 먼저 "이렇게 이해했는데 맞나요?"라고 요청 내용을 정리해서 되물은 뒤 확인받고 진행한다.
이해한 내용을 짧게 요약하고, 애매한 부분이 있다면 선택지를 제시하는 방식을 사용한다.
특히 기획서 7장("아직 정해지지 않은 것") 항목과 관련된 요청은 값을 임의로 지어내지 말고 반드시 먼저 확인한다.

### 2-4. Unity 환경 준수
모든 코드는 Unity(C#)에서 바로 사용 가능한 형태로 작성한다.
MonoBehaviour, ScriptableObject, Unity 이벤트 시스템 등 Unity의 표준 패턴을 우선 활용한다.
에디터에서 값 조정이 필요한 부분(난이도 곡선, 페널티 강도, 문제 확률 등)은 `[SerializeField]`나 ScriptableObject로 노출시켜 기획자가 코드 수정 없이 튜닝할 수 있게 한다.

### 2-5. 코드 수정 시 변경 사항 명시
기존 파일을 수정할 때는 수정 전 코드와 수정 후 코드를 함께 제시하고, 어떤 부분이 왜 바뀌었는지 설명한다.
가능하면 변경된 줄을 명확히 구분해서(예: 주석 표시, diff 형태) 한눈에 변화를 파악할 수 있게 한다.

---

## 3. 난이도 설계 철학 (기획서 기반 확정본)

- **문제 제시 방식 — 단일 질문 객관식** (2026-08-24 확정, 기획서 5-3을 대체함)
  - 문제는 **최종 답 하나만 묻는 단일 질문**으로 제시한다. 풀이 과정을 여러 단계로 쪼개 순서대로 묻지 않는다.
  - **어떤 개념/공식을 쓸지 객관식으로 묻지 않는다.** 그 판단은 플레이어가 **계산기에서 어떤 특수 기능을 고르는지**로 드러난다 — 인수분해를 써야 할 상황에 인수분해 버튼을 고르는 것 자체가 개념 판단이다 (5-3-1 원칙의 실제 구현 형태).
  - 계산기 사용은 **완전한 선택**. 암산으로 바로 답해도 되고, 계산기를 열어 풀어도 된다.
  - 숫자 입력이 번거로운 것(특수 기능, 인수분해 등)은 **버튼 UI**로 제시한다 (4-3 블록 입력 UI 참고).
  > ⚠️ **기획서 5-3에서 변경됨** — "계산 과정을 여러 단계로 쪼갠 스캐폴딩"과 **"오답 소거"(틀릴 때마다 오답 보기 제거)는 둘 다 폐지**됐다. 스캐폴딩 단계 배열이나 오답 소거 로직을 만들지 말 것.

- **입문 구간 (튜토리얼~챕터1):** 오답 페널티가 상징적 수준으로 약함(기획서 5-4). **이 구간엔 계산기 특수 기능이 필요 없는 문제만 출제**한다 (2026-08-24 확정) — 해금 상태에 따라 문제 형태를 분기시키는 로직을 만들지 말고, **문제 풀 자체를 그렇게 구성**할 것.

- **오답 페널티는 2단계다 — 마나가 먼저, 체력이 나중.** (기획서 5-4, 2026-08-24 확정)
  1. 큰 스킬 발동 시 마나를 소모한다. **오답이면 마나를 더 많이 소모하고, 스킬도 발동되지 않는다** — 자원만 날리는 이중 손해.
  2. **마나가 고갈된 상태에서 오답이면 체력이 깎인다.**

  마나가 1차 완충재, 체력이 2차 실손해다.
- **챕터별 강도 커브는 "단계별 차등" 대신 두 축으로 만든다** (기획서 5-4 개편) — 문제가 단일 질문이 되며 옛 "초반 판단 단계는 약하게, 최종 계산 단계는 크게" 구분이 성립하지 않게 되어 이렇게 대체됐다.
  1. **후반 챕터일수록 스킬에 필요한 마나 총량 자체가 커진다** — 실수 한 번의 기회비용이 자연히 무거워진다.
  2. **연속 오답일수록 깎이는 마나/체력이 누적해서 커진다** — 같은 구간에서 틀릴 때마다 그 전보다 더 아프다.
  이 두 축의 조합이 챕터별 페널티 커브를 만든다. 마나 총량 커브·누적 배수의 구체 수치는 미정(5장 참고).
- **막혀서 게임오버 되는 것은 정상 결과다.** 오답 소거 같은 별도 안전장치는 두지 않는다. 대신 **애초에 못 풀 수가 없도록 문제 풀을 밸런싱**하는 쪽으로 막힘을 방지한다 (기획서 5-3 참고). 그래도 막히면 정상적으로 게임오버 → 재도전(5-5-1)으로 이어진다.

- **계산 실행 vs 개념 판단 역할 분담 (5-3-1) — 문제 설계의 최상위 원칙.**
  이 게임에는 만능 계산기가 있으므로, 전투 중 문제는 **계산기가 대신할 수 없는 "개념 판단" 위주**로 설계한다.
  - **문제** → 어떤 개념을 아는지, 어떤 공식을 언제 써야 하는지, 개념을 구별/판단할 수 있는지
  - **계산기** → 확정된 개념/공식을 실제로 적용해 정확한 결과를 뽑는 실행 도구
  - **판정 기준:** "계산기 없이 암산/직관으로 15초 안에 승부가 나는가?" — 걸리면 적합, 계산기를 켜야만 풀리면 부적합.
  - "짧은 확인계산 → 개념 판단"으로 이어지는 문제(예: 판별식 계산 후 부호로 근의 개수 판단)는 허용. 순수 다단계 계산 문제는 지양.

- **숙련도 시스템:** 같은 **큰 스킬**을 반복 사용하면 문제 난이도가 낮아지고, 최종적으로는 문제 없이 즉시 발동(평타화)된다. 본편 전투에서 자연 누적되며, **스킬 숙련도 던전**에서 집중 파밍할 수 있다. (기획서 5-1, 6-1, 6-3)

- **약점 보충 시스템(개념 트리):** 특정 개념에서 막히면 처음부터가 아니라 해당 개념 + 직결된 선수 개념 1~2단계만 콕 집어 보충한다. 이를 위해 개념 간 선후관계를 담은 데이터 구조(개념 트리/그래프)가 반드시 필요하다. (기획서 4장)

- **정령의 도움:** 동반자 정령은 전투 중 **플레이어가 능동적으로 사용하는 자원 제한형 특수 스킬**로 구현되며, 문제 풀이 힌트를 제공한다. 정답을 바로 알려주지 않고 방향성만 짚어준다. (기획서 5-1-1)
  > ⚠️ **구 설계에서 변경됨** — 이전의 "오답 시 자동으로 짧은 대사 + 보기 축소" 방식이 아니라, 유저가 자원을 소모해 발동하는 스킬이다. 계산기 특수기능 무제한화로 줄어든 "자원 관리의 재미"를 이쪽으로 옮긴 것이므로, 자동 발동으로 만들면 도입 목적이 사라진다.

- **보스전 — 단원평가 기믹:** 각 보스는 자신이 지키는 마을의 단원(기획서 3-2)에서 문제를 출제한다. 이 정답/오답 판정이 **곧바로 보스 기믹 3종(즉사 장판 / 체력 깎임 / 패턴 강화·완화)의 발동 트리거**가 된다 — 별도의 판정 레이어를 새로 만들지 말고, 기존 기믹의 출제 범위를 해당 마을 단원으로 고정하는 방식으로 구현할 것. (기획서 3장, 5-5)

- **어려운 구간 대응:** 챕터3처럼 개념 자체가 어려운 구간은 오답률을 억지로 낮추려 하지 않고, "안 죽게"(스탯/회복 밸런스)와 "안 지치게"(정답 보상 극대화)로 대응한다. (기획서 5-4)

- **최종 목표 난이도:** 모의고사 18번 수준 (킬러문항급 지양, "노력하면 풀리는 어려움" 유지). (기획서 3장)

---

## 4. 핵심 게임 시스템 (기획서 기반 확정본)

### 4-1. 전투 구조 (기획서 5장)
- **평타:** 버튼만 누르면 즉시 발동, 문제 없음. 전투에 "쉴 틈"과 리듬감을 만드는 역할.
- **스킬 — 위력에 따라 2계층으로 나뉜다 (기획서 5-1):**
  - **평타급 스킬(약한 스킬):** 평타처럼 문제 없이 즉시 발동.
  - **큰 스킬:** 발동 시 대응하는 수학 문제가 등장하며, **정답을 맞혀야 발동**한다. 오답이면 마나를 더 쓰고 **발동도 실패**한다 (3장 참고). 계산기 특수기능(근의공식·인수분해 등)과 직결되는 스킬군.
  - 두 계층을 가르는 구체적 기준(데미지 배율, 쿨타임 등)은 **미정** — 기획서 7장.
- **마나 = 큰 스킬의 자원 게이트** (기획서 5-1). 큰 스킬은 마나를 소모하며, 오답 시 더 많이 소모한다(3장 참고). 마나가 마르면 평타 + 평타급 스킬 위주로 밀려나며, 이것이 5-6 "노룩 클리어"(챕터3부터 도전 가능)의 구조적 근거다.
- **회피:** 숙이기/점프 2종. 몬스터 공격엔 수학 문제가 없고, 텔레그래프(전조 동작)를 보고 액션으로 피하는 방식.
- **검/마법 빌드:** 검(계산 계열) · 마법(**도형 계열**) 자유 혼합 투자. 특정 보스는 특정 계열에 효율 저하(완전 면역 아님)를 가짐. 삼각함수는 양쪽에 걸치는 예외 케이스. (기획서 5-2)
  > ✅ **2026-08-24 확정 — 확률통계는 전면 삭제.** 기획서 5-2가 마법을 "도형·확률통계 계열"이라 적은 것은 3-2 마을 단원표·6-2의 확통 제외와 충돌했고, **확통을 모두 빼고 도형만 남기는 것으로 확정**됐다. 확률·통계 관련 개념·스킬·문제·계산기 기능을 **일절 만들지 말 것.**
- **몬스터 등급:** 일반 / 정예 / 보스 등급 체계 (세부 명칭·단계 수 **미정**). (기획서 5-8)

### 4-2. 진행 구조 (기획서 3장, 9장)
- **RPG 구조:** 죽어도 처음부터 다시 시작하지 않음. 진행은 유지되고 리스폰/재도전.
- **챕터:** 튜토리얼 → 챕터1(중1~중2) → 챕터2(중3~고1) → 챕터3(고2). 고3 범위 스코프 제외.
- **마을 = 진행 및 단원의 기본 단위. 총 8개** — 챕터1: 5개, 챕터2: 3개, 챕터3: **0개**.
  - 마을 하나 = 해당 학년의 "영역 묶음(수와 연산 · 문자와 식 · 함수 · 기하)" 단위.
  - 마을별 단원 배치는 **기획서 3-2 표를 그대로** 데이터 구조(ScriptableObject)에 반영할 것. 임의로 단원을 추가/삭제하지 말 것.
  - **챕터3은 마을이 없다** (기획서 3-3) — 재앙으로 마력이 변질돼 기존 개념으로 상대할 수 없는 신종 몬스터가 출몰하고, 이를 이기려면 새 개념(고2 수학)을 얻어야 하는 **강제 해금 구조**. 챕터3의 진행 게이트는 **미정**.
- **마을 퀘스트 · 촌장 증표 (기획서 3-4):** 몬스터 퇴치 → 증거품을 촌장에게 → 증표 지급. 증표는 스킬북 교환 + 다음 지역 이동 게이트로 기능.
  - (미정) 이 "스킬북"이 6장 계산기 특수기능 해금과 같은 것인지 별도 시스템인지.
- **보스:** 챕터당 일반 보스 2~3마리, 학습 곡선 고비 지점에 배치. 챕터2는 여기에 더해 라이벌2 대결이 클라이맥스로 추가. 보스 기믹은 일반 전투 페널티와 별도 룰(기획서 5-5).

### 4-3. 계산기 시스템 (기획서 6장) — **대폭 개편됨**

> **계산기 사용은 완전한 선택이다** (2026-08-24 확정). 써도 되고 안 써도 된다 — 암산으로 바로 답할 수 있으면 열 필요가 없다. 어떤 문제도 "계산기를 반드시 켜야만 풀리는" 형태로 만들지 말 것 (5-3-1의 15초 기준과 같은 취지).
- **1계층 · 기본 계산기:** 사칙연산 + 특수 기능(근의공식 / 인수분해 / 지수·로그 / 삼각함수 / 급수 / 미분)이 **전부 여기 포함**. **해금 조건은 스토리 진도뿐** — 플레이어의 문제 풀이 실적은 조건이 아니다 (기획서 6-4). 해금 이후에는 **사용 횟수 제한 없이 무제한 사용**.
  > ⚠️ **구 설계에서 변경됨** — "문제당 3~4회 제한", "본편 플레이로 자연 리필", **"계산기 게이지"** 개념은 전부 **폐지**. 계산기 사용 횟수·게이지를 세는 자원/카운터를 만들지 말 것.
- **2계층 · 스킬 숙련도 던전** (구 "계산기 던전"에서 용도 변경): 특수기능이 무제한화되어 기존 존재 이유가 사라졌으므로, **큰 스킬의 숙련도를 집중 파밍하는 선택적 콘텐츠**로 전환. 본편과 동일한 전투 시스템 사용. 완전히 선택 사항이며 강제 파밍 요구 금지.
- **조작 방식 — 블록 입력 UI (기획서 6-5, 신규 확정):**
  - **원칙: 타이핑 없이, 선택/클릭/블록 배치만으로 계산이 완결되어야 한다.** 전투 중 물리 키보드 숫자 입력은 리듬을 끊으므로 배제.
  - 블록 2종: **숫자 블록**(사칙연산용 순수 숫자) / **항·식 블록**(문제에 등장하는 항 단위 — `x`, `x+3`, `2x²` 등을 미리 조각내어 제공).
  - 특수 기능은 조합된 입력에 **기능 버튼(원터치)**을 눌러 결과를 받는다. **계산 "과정"을 블록으로 단계별 노출하지 말 것** — 과정을 유추할 수 있으면 그 자체가 힌트가 되어 개념 판단 문제의 긴장감이 희석된다. 계산기는 **블랙박스형 실행 도구**로 유지.
  - **되돌리기**("처음으로" / "1단계 취소")는 항상 열어둔다. 페널티와 무관한 조작 편의 기능.
  - **유효성 검증 2단계:** ① 문법적으로 불가능한 조합(상수에 인수분해 등) → 실행 차단 + **오답 처리에 포함**(판단력 부족으로 봄) ② 문법적으로 가능하나 정답 경로가 아닌 조합 → 정상 실행되지만 **오답 처리**.

### 4-4. 계산기 화살 (기획서 5-8) — 선택 요소
- 전투 중 특수 키로 계산기("룬 장갑")에서 화살 발사 → 몬스터 명중 시 문제 화면 전환 → 정답 시 해당 몬스터에게 **디버프**(받는 피해 증가 등).
- **필수 아닌 보조 콘텐츠** — 화살을 안 써도 전투 진행에 지장이 없어야 한다 (5-6 노룩 클리어 철학과 동일).
- 5-2 상성 시스템과는 **병행** 관계: 상성이 정한 기본 데미지 배율 위에 화살 디버프가 추가로 얹힌다. 하나로 합치지 말 것.
- 명칭 · 조준 방식(자동/수동) · 사거리 · 쿨타임 · 자원 소모 · 디버프 수치는 전부 **미정**.

### 4-5. 데이터 구조 설계 시 유의사항
- **개념 트리(4장), 마을·단원 매핑(3-2), 검/마법 계열 구분(5-2)은 서로 참조하는 구조**이므로, `Data/` 폴더에서 일관된 ID 체계로 연결할 것 (개념 ID가 단원표·트리·마을·보스 출제범위에서 공통으로 쓰이도록).
- 보스의 출제 범위는 **마을 ID로 참조**한다 (단원평가 기믹, 3장/5-5) — 보스마다 단원 목록을 따로 하드코딩하지 말 것.
- **더 이상 존재하지 않는 시스템** (코드/데이터로 만들지 말 것): 계산기 특수기능 사용 횟수·리필, **계산기 게이지**, 계산기 던전(→ 스킬 숙련도 던전으로 대체), 검↔마법 시너지 콤보, **확률·통계 관련 일체**.
  - 반대로 **마나는 존속한다** — 기획서 본문에 없더라도 지우지 말 것 (3장·4-1 경고 참고).

---

## 5. 아직 확정되지 않은 것 (구현 시 주의)

기획서 7장에 명시된 미정 사항들은 **하드코딩하지 말고 반드시 ScriptableObject/설정값으로 노출**해서 나중에 기획자가 채워 넣을 수 있게 할 것. 특히:

- "평타급 스킬"과 "큰 스킬"을 가르는 기준 (데미지 배율, 쿨타임 등)
- **마나 총량, 회복 속도, 큰 스킬별 기본 소모량, 오답 시 추가 소모 배수, 챕터별 마나 총량 커브**(후반일수록 커지는 정도)
- **연속 오답 누적 배율** — 몇 번째 오답부터 얼마나 커지는지, 누적이 초기화되는 시점(전투 종료? 정답 1회?)
- **마나 고갈 상태에서의 오답 체력 페널티** 강도
- 챕터별 정답 보상의 종류/수치
- 정령의 도움 스킬의 힌트 형태, 재충전 방식·횟수
- 계산기 블록 UI 세부 (블록 배치 방식, 기능 버튼 위치/단축키, 고난이도 식에서의 확장성)
- 계산기 화살의 명칭 · 조준 방식 · 사거리 · 쿨타임 · 자원 · 디버프 수치·지속시간
- 몬스터 등급 기준 및 명칭, 챕터별 등급 분포
- 즉사장판 회피 판정 프레임 수, 챕터별 액션 난이도 커브
- 라이벌1/2 위력감소·명중실패 확률
- 보스 패턴 강화/완화 단계 상한선
- 이지모드 트리거 기준
- 스킬 숙련도 상승 배율·수치, 숙련도 던전 자체 난이도
- 게임오버 후 복습 시스템의 보상·문제 개수
- 챕터3의 진행 게이트 (마을 대체)
- 최종보스 세부 수치 (제한시간 감소폭, 장판 크기, 페이즈 전환 조건, 정령화 지속시간)

값이 필요한 코드를 작성할 때 이 목록에 해당하면, 임의의 숫자를 넣지 말고 사용자에게 먼저 확인할 것.

**2026-08-24 확정되어 기획서 원문(5-1·5-2·5-3·5-4·6-4)에 이미 반영된 사항:** 마나 2단계 페널티(오답 시 마나 추가 소모+발동 실패 → 마나 고갈 시 체력), 마나 총량 커브 + 연속 오답 누적으로 챕터별 강도 조절, 마법 계열은 도형만(확통 삭제), 계산기 게이지 폐지, 특수기능은 스토리 진도로만 해금, 문제는 단일 질문 객관식(스캐폴딩·오답소거 폐지), 개념 판단은 계산기 기능 선택으로 대체, 초반 구간은 특수기능 불필요한 문제만 출제. 자세한 경위는 `Docs/개발_로드맵.md` 부록 B-3 참고.

---

## 6. 참고 문서

- **기획서 원본:** `Docs/수학로그라이크_기획서_updated__1_3.md` — 전투/계산기 시스템, 챕터·마을 구조, 수학 단원 스코프, 설문 인사이트 등 시스템 설계 근거. 코드 작성 전 관련 섹션을 항상 먼저 확인할 것.
- **스토리 문서:** `Docs/수학로그라이크_스토리_2.md` — 세계관, 인물 서사(주인공/라이벌1·2/정령/왕/최종보스), 챕터별 스토리 흐름, 엔딩·에필로그. 연출·대사·NPC 관련 작업 시 참고.
- **개발 로드맵:** `Docs/개발_로드맵.md` — 무엇을 어떤 순서로 만들지(M0~M7), 마일스톤별 완료 기준, 미정 수치 확인 시점. 작업 착수 전 현재 마일스톤을 확인할 것.

> 이 CLAUDE.md는 기획서/스토리 문서가 갱신될 때마다 함께 갱신되어야 함 — 내용이 어긋나면 사용자에게 알리고 동기화할 것.
> 문서 파일명이 바뀌면 이 6장의 경로와 로드맵 서문의 경로도 함께 고칠 것 (과거 두 번 깨진 이력 있음).
