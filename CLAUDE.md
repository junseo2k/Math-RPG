# CLAUDE.md — 수학 전투 RPG 개발 지침

> 이 문서는 Claude Code가 이 프로젝트에서 코드를 작성/수정할 때 항상 따라야 하는 지침입니다.
> 세부 수치·스토리·시스템 설계는 이 문서가 아니라 `Docs/수학로그라이크_기획서_updated.md`(이하 "기획서")가 원본입니다. 이 문서와 기획서가 충돌하면 **기획서가 우선**하며, 이 문서를 기획서에 맞게 갱신해야 합니다.

---

## 1. 프로젝트 개요

- **장르:** 2D 전투 RPG (로그라이크 아님 — 죽어도 처음부터 다시 시작하지 않고, 리스폰/재도전하되 진행은 유지되는 구조. 기획서 9장 참고)
- **타겟:** 수포자(수학을 포기한 사람들)
- **핵심 루프:** 수학 문제를 풀어 전투력을 얻고, 그 힘으로 몬스터/보스와 싸운다
- **목표 철학:** 처음엔 부담 없는 난이도로 시작해서, 플레이하는 동안 자연스럽게 수학 개념을 익히며 "배움의 재미"와 "전투의 재미"를 동시에 느끼게 한다.
- **개발 우선순위:** 전투/타격감이 수학 문제보다 우선순위가 높다. "수학 문제가 곁들여진 진짜 액션 게임"이 목표 포지셔닝 — 문제 로직 없이 순수 액션(평타-스킬-회피 루프)만으로도 재미가 검증되어야 한다. (기획서 5-7)
- **개발 환경:** Unity (C#), 2D

---

## 2. 개발 진행 원칙 (반드시 준수)

### 2-1. 큰 수정 시 사전 확인
여러 파일에 걸친 수정, 핵심 시스템(전투 로직, 데이터 구조, 저장 시스템 등) 변경, 혹은 기존 기능의 동작 방식을 바꾸는 수정이 필요할 경우, 먼저 어떤 부분을 왜 수정하려는지 설명하고 사용자의 승인을 받은 후 진행한다.
단순 버그 픽스, 오타 수정처럼 영향 범위가 명확히 작은 경우는 바로 진행해도 된다.

### 2-2. 기능별 모듈화
절대 하나의 거대한 스크립트에 모든 로직을 넣지 않는다. 최소한 아래와 같이 폴더/네임스페이스 단위로 분리한다:

```
Combat/       - 전투 시스템 (턴/실시간 진행, 데미지 계산, 마나 소모, 상태이상 등)
MathQuiz/     - 수학 문제 생성, 난이도 조절(스캐폴딩·오답소거), 채점 로직
Player/       - 플레이어 스탯, 성장(숙련도), 인벤토리
Enemy/        - 일반 몬스터 및 보스 AI, 패턴, 상성
Progression/  - RPG 진행, 맵/노드 구조, 약점 보충 노드(개념 트리)
UI/           - 각 화면별 UI 컨트롤러
Data/         - ScriptableObject 기반 데이터 정의 (문제 세트, 몬스터 스탯, 개념 트리 등)
Core/         - 게임 매니저, 이벤트 시스템 등 공통 기반
```

클래스 하나가 여러 책임을 갖지 않도록 하고, 시스템 간에는 이벤트나 인터페이스로 느슨하게 연결한다.

### 2-3. 모호한 요청 처리
요청이 모호하거나 여러 해석이 가능할 경우, 임의로 추론해서 진행하지 말고, 먼저 "이렇게 이해했는데 맞나요?"라고 요청 내용을 정리해서 되물은 뒤 확인받고 진행한다.
이해한 내용을 짧게 요약하고, 애매한 부분이 있다면 선택지를 제시하는 방식을 사용한다.
특히 기획서 7장("아직 정해지지 않은 것") 항목과 관련된 요청은 값을 임의로 지어내지 말고 반드시 먼저 확인한다.

### 2-4. Unity 환경 준수
모든 코드는 Unity(C#)에서 바로 사용 가능한 형태로 작성한다.
MonoBehaviour, ScriptableObject, Unity 이벤트 시스템 등 Unity의 표준 패턴을 우선 활용한다.
에디터에서 값 조정이 필요한 부분(난이도 곡선, 마나 소모량, 문제 확률 등)은 `[SerializeField]`나 ScriptableObject로 노출시켜 기획자가 코드 수정 없이 튜닝할 수 있게 한다.

### 2-5. 코드 수정 시 변경 사항 명시
기존 파일을 수정할 때는 수정 전 코드와 수정 후 코드를 함께 제시하고, 어떤 부분이 왜 바뀌었는지 설명한다.
가능하면 변경된 줄을 명확히 구분해서(예: 주석 표시, diff 형태) 한눈에 변화를 파악할 수 있게 한다.

---

## 3. 난이도 설계 철학 (기획서 기반 확정본)

기존 프롬프트의 2-6은 초안이었고, 실제로는 기획서에서 아래와 같이 구체화됨:

- **입문 구간 (튜토리얼~챕터1):** 마나 소모 페널티가 약함(기획서 5-4). 문제는 단계별 스캐폴딩(계산 과정을 여러 단계로 쪼갠 객관식, 5-3)으로 제시되어 한 번에 "응용"을 요구하지 않는다.
- **오답 페널티는 체력이 아니라 마나다.** 오답 시 체력이 깎이는 게 아니라, 스킬 발동에 필요한 마나를 더 많이 소모하면서 발동에 실패한다(마나만 날아감). 보스 공격에 맞아서 체력이 깎이는 것과는 별개 시스템이다. (기획서 5-1-1, 5-4)
- **숙련도 시스템:** 같은 스킬을 반복 사용하면 문제 난이도가 낮아지고, 최종적으로는 문제 없이 즉시 발동(평타화)된다. 숙련도가 오를수록 정답 시 마나 소모도 줄어든다. (기획서 5-1)
- **약점 보충 시스템(개념 트리):** 특정 개념에서 막히면 처음부터가 아니라 해당 개념 + 직결된 선수 개념 1~2단계만 콕 집어 보충한다. 이를 위해 개념 간 선후관계를 담은 데이터 구조(개념 트리/그래프)가 반드시 필요하다. (기획서 4장)
- **정령의 힌트:** 정답을 바로 알려주지 않고 방향성만 짚어준다. 평소 전투 중 오답 시엔 짧은 대사 + 보기 축소만 제공(상호작용 없음, 재시도 흐름 유지). 심층 역질문(소크라테스식 문답)은 플레이어가 명시적으로 정령에게 대화를 요청했을 때만 발동 — 매 오답마다 별도 질문-응답을 끼워 넣지 않는다. (기획서 2-4)
- **보스전:** 그동안 누적된 개념을 종합적으로 사용하는 문제로 구성. 챕터3처럼 개념 자체가 어려운 구간은 오답률을 억지로 낮추려 하지 않고, "마나가 안 마르게"(총량/회복 밸런스)와 "안 지치게"(정답 보상 극대화)로 대응한다. (기획서 5-4)
- **최종 목표 난이도:** 모의고사 18번 수준 (킬러문항급 지양, "노력하면 풀리는 어려움" 유지). (기획서 3장)

---

## 4. 핵심 게임 시스템 (기획서 기반 확정본)

기존 프롬프트의 3장은 초안이었고, 실제 확정된 시스템은 다음과 같음 — 구현 시 반드시 아래 규칙을 따를 것:

### 4-1. 전투 구조 (기획서 5장)
- **평타:** 버튼만 누르면 즉시 발동, 문제 없음.
- **스킬:** 발동 시 대응하는 수학 문제 등장 + 마나 소모. 정답 → 마나 적게 소모하고 발동 성공. 오답 → 마나 많이 소모하고 발동 실패(스킬 안 나감).
- **마나:** 스킬 사용의 자원 게이트. 마나 포션은 희귀 아이템. 마나 고갈 시 평타만 사용 가능(하드코어 "노룩 클리어" 컨셉과 연결, 5-6).
- **회피:** 숙이기/점프 2종. 몬스터 공격엔 수학 문제가 없고, 텔레그래프(전조 동작)를 보고 액션으로 피하는 방식.
- **검/마법 빌드:** 검(계산 계열) · 마법(도형·삼각 계열) 자유 혼합 투자. 특정 보스는 특정 계열에 효율 저하(완전 면역 아님)를 가짐. (기획서 5-2)

### 4-2. 진행 구조 (기획서 3장, 9장)
- **RPG 구조:** 죽어도 처음부터 다시 시작하지 않음. 진행은 유지되고 리스폰/재도전.
- **챕터:** 튜토리얼 → 챕터1(중1~중2) → 챕터2(중3~고1) → 챕터3(고2). 고3 범위 스코프 제외.
- **수학 단원 스코프:** 검/마법 계열별 챕터당 단원 목록, 시너지 콤보, 제외 단원 사유는 기획서 11장 표를 그대로 데이터 구조(ScriptableObject)에 반영할 것. 임의로 단원을 추가/삭제하지 말 것.
- **보스:** 챕터당 2~3마리, 학습 곡선 고비 지점에 배치. 보스 기믹은 일반 전투 페널티와 별도 룰(즉사장판/체력깎임/패턴 강화-완화, 기획서 5-5).

### 4-3. 계산기 시스템 (기획서 6장)
- 기본 계산기(무제한) → 특수 기능(스킬트리 상위 자동화, 사용횟수 제한) → 계산기 던전(특수 기능 무한 파밍용 선택 콘텐츠)의 3계층 구조.
- 특수 기능은 본편 플레이만으로 자연 리필됨 — 강제 파밍 요구 금지.

### 4-4. 데이터 구조 설계 시 유의사항
- 개념 트리(4장), 수학 단원 스코프(11장), 검/마법 시너지 콤보(11-4)는 서로 참조하는 구조이므로, `Data/` 폴더에서 이 셋을 일관된 ID 체계로 연결할 것 (예: 개념 ID가 단원표·트리·콤보 테이블에서 공통으로 쓰이도록).
- 마나 시스템과 계산기 특수 기능 시스템은 **별개의 자원**이다 (기획서 5-1-1). 하나의 자원으로 합치거나 혼동하지 말 것.

---

## 5. 아직 확정되지 않은 것 (구현 시 주의)

기획서 7장에 명시된 미정 사항들은 **하드코딩하지 말고 반드시 ScriptableObject/설정값으로 노출**해서 나중에 기획자가 채워 넣을 수 있게 할 것. 특히:
- 마나 소모량, 배수, 챕터별 커브
- 계산기 특수 기능 사용 제한 횟수, 리필량
- 즉사장판 회피 판정 프레임 수, 챕터별 액션 난이도 커브
- 라이벌1/2 위력감소·명중실패 확률
- 보스 패턴 강화/완화 단계 상한선
- 이지모드 트리거 기준

값이 필요한 코드를 작성할 때 이 목록에 해당하면, 임의의 숫자를 넣지 말고 사용자에게 먼저 확인할 것.

---

## 6. 참고 문서

- **기획서 원본:** `Docs/수학로그라이크_기획서_updated.md` — 세계관, 스토리, 전투/계산기 시스템, 수학 단원 스코프, 설문 인사이트 등 모든 설계 근거가 여기 있음. 코드 작성 전 관련 섹션을 항상 먼저 확인할 것.
- **개발 로드맵:** `Docs/개발_로드맵.md` — 무엇을 어떤 순서로 만들지(M0~M7), 마일스톤별 완료 기준, 미정 수치 확인 시점. 작업 착수 전 현재 마일스톤을 확인할 것.
- 이 CLAUDE.md는 기획서가 갱신될 때마다 함께 갱신되어야 함 — 기획서 내용과 이 문서가 어긋나면 사용자에게 알리고 동기화할 것.
